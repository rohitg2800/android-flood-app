<!-- android/app/src/main/AndroidManifest.xml -->
<!-- ProfileInstaller receives an ACTION_INSTALL_PROFILE intent on every
     app startup; the receiver below is a no-op replacement that prevents
     the default ProfileInstaller background work from stalling debug launches. -->
