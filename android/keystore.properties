# SECURITY: Real passwords must NEVER be committed to git.
# Set these as GitHub Actions secrets and inject at build time.
# See .github/workflows/release.yml for injection pattern.
#
# Local dev: copy this file to keystore.local.properties and fill real values.
# keystore.local.properties is .gitignored.

storeFile=../keystore.jks
storePassword=KEYSTORE_STORE_PASSWORD_FROM_ENV
keyAlias=equinox_bh
keyPassword=KEYSTORE_KEY_PASSWORD_FROM_ENV
